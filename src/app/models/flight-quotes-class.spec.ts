import { FlightQuotesClass } from './flight-quotes-class';

describe('FlightQuotesClass', () => {
  it('should create an instance', () => {
    expect(new FlightQuotesClass()).toBeTruthy();
  });
});
